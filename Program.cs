using System;

class Program
{
    static void Main(string[] args)
    {
        const double pi = 3.14;
        double r = Convert.ToDouble(Console.ReadLine());
        double p = pi * r*r;
        Console.WriteLine(p);
        
    }
}